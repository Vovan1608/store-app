import BooksListContainer from '../booksListContainer';
import ShoppingCartTable from '../shoppingCartTable';

const Cart = () => {
    return (
        <ShoppingCartTable />
    );
};

export default Cart;
