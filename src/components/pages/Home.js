import BooksList from '../booksList';
import ShoppingCartTable from '../shoppingCartTable';

const Home = () => {
    return (
        <div>
            <BooksList />
            <ShoppingCartTable />
        </div>
    );
};

export default Home;